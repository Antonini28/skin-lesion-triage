# ─────────────────────────────────────────────────
#  Skin Lesion Triage API — Python Dependencies
# ─────────────────────────────────────────────────
# Install with:
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements.txt

fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic>=2.13.4,<3
pydantic[email]>=2.13.4,<3
Pillow==12.3.0
huggingface-hub==1.23.0
timm==1.0.28
numpy<3.0

# ── Auth & Database ──
sqlalchemy==2.0.51
python-jose[cryptography]==3.5.0
email-validator==2.3.0

# ── DermBot — RAG + LLM ──
# Retrieval uses Gemini embeddings + numpy cosine search (no local ML model),
# so it fits Render's free 512 MB tier. See app/services/rag_service.py.
# Needs a modern SDK for gemini-embedding-001 + output_dimensionality/task_type.
google-genai==2.11.0

# ── PyTorch CPU-only wheels ──
# Install with: pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements.txt
torch==2.13.0
torchvision==0.28.0
